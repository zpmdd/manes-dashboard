# 数字雕塑与公共玻璃边框验收 · 2026-09-18

用户选定数字雕塑方案后，明确去除车辆模型，并要求数字沿用项目现有字体。最终使用 Michroma 真文本、金属渐变及浮雕投影，保留车辆源计算的数量、单位和“显示全部车辆”入口。数字按位数与卡片尺寸自适应，未引入新字体或运行时图片。

公共边框覆盖所有图表卡片的 glass / soft / solid 材质、地图工具条、弹窗及车辆悬浮信息框；细亮边、角部反射和投影共用主题变量。边缘反射层不接收指针事件，卡片原有半透明背景和数据交互保留。

验收使用 Chrome 扩展连接，针对两版正式构建：

- 道研：1920×1080 全国、车辆 3 的朝阳区定位、列表勾选、信息浮层及浅色离线道路底图。总数为 5，卡片内无车辆图标，数字计算样式确认为 Michroma。
- 基线：全国五张卡片（指标、仪表、趋势、排行、表格），覆盖三种材质；丹江口道路底图与空排行状态。原有普通指标排版保持不变。
- 深海青碧主题：在编辑草稿中验证数字、反射边缘和三维场景同步换色，并撤销草稿还原。
- 两版 390px 窄屏：无页面横向溢出。道研数字与单位均在卡片内，0、12、1,234、123,456 的临时文本布局探针全部通过；探针在同次执行中恢复真实数值，不修改数据源。
- 两版浏览器控制台错误均为 0；临时视口覆盖已清理，专用测试标签页已关闭。

已将选定参考图与修改前、最终截图对照，确认去车与统一字体两项用户修正生效。截图和检查结果见 [验收证据](evidence/sculpted-metric/)，[最终卡片细节](evidence/sculpted-metric/metric-detail.png)。

`fnm exec --using=v24.15.0 npm run verify` 通过：121 项单元测试、地图检查、双项目构建、4 项 Sites 检查及产物隔离检查。构建仍有既有的大体积 chunk 提示，无构建失败。
