# 可复用编辑器检查记录 · 2026-09-12

本轮交付自由画布、12 种业务组件、模板管理和 JSON / CSV / HTTP 数据源接入。完成本地构建、自动检查及 Chrome 视觉与交互验收；不代表生产部署或现场验收。

## 自动检查

运行环境为 Node.js 25.9.0。提交前执行：

| 检查 | 结果 |
|---|---|
| `npm test` | 35 项配置、数据源、模板、布局、真实指针事件与组件渲染测试通过；地图取景的 40 个投影角点及既有 hover、材质、点击检查通过 |
| `npm run check` | 3238 个索引、364 个地图集合、1,151,634 个环顶点，以及层级、内环、多岛和道路引用检查通过 |
| `npm run test:sites` | 4 项通过；Sites 兼容文件保持完整 |
| `npm run build` | 通过；编辑属性/模板、数据源、全局配置面板按需拆包，主 JS gzip 约 389 KB，保留既有大包提示 |
| `git diff --check` | 通过 |

检查覆盖 v1 迁移、v2 严格校验、无效导入保留现状、数值边界、CSV 转义、请求取消与过期结果隔离、失败保留同范围快照、隐藏页面暂停轮询、拖动单步历史、撤销/重做及 Esc/失去指针捕获时恢复。提交前修复区域编码列被过滤的问题，真实组件渲染检查覆盖全部 7 列、仅编码列以及编码前导零。

## 已完成的浏览器操作

使用 Chrome 和本地生产预览，主要检查 1920×1080、2560×1261。过程中将参考对照图与实际页面截图一起检查，保留烟灰玻璃、暖白/香槟、共享三维背景；未更改反射、阴影和地图几何质量参数。

- 添加公告并修改多行文字；拖动位置 39,27 → 46,34，撤销回到起点；缩放 28×32 → 34×37。
- 复制、删除、撤销/重做、图层顺序和锁定可用；隐藏组件在预览消失，返回编辑后可以恢复。
- 双侧监测模板可载入；12 种组件组合可通过 JSON 导入并渲染。
- 示例 HTTP 文件读取 6 行，绑定指标显示 3,841；改为不存在的数值字段后显示具体错误，未回退成演示值。
- CSV 源读取两行 862、715，绑定表格后数值升序为 715、862；添加 CSV 源和组件没有额外重读原 HTTP 源。
- 本地另存模板、实际 JSON 下载及重新导入可用；导出内容通过配置校验，包含 6 个组件和 2 个数据源。无效文件被拦截，草稿保留。
- 保存画布并刷新后，HTTP 指标和 CSV 表格绑定保持；全局标题修改可以应用到编辑草稿。

## 最终复核与修复

锁屏中断后已恢复 Chrome 并完成下列复核：

- 未保存退出时出现原生确认框；取消确认后保留“接口复用验证”标题草稿，确定后恢复已保存标题。再加载生产预览保持原配置。
- 运行总览、双侧监测、数据分析三套模板及十二组件组合均进行实际页面检查；图表名称、表格和字号下限复核通过。
- 修复柱图沿用时序末尾截取的问题：柱图与条形图显示输入前 N 条，折线/面积图保留末 N 条；SSR 检查覆盖该差异且不自动排序输入。数据分析模板显示澳门、香港、台湾、新疆、宁夏五个排名项。
- 首屏资源记录不包含三个编辑面板；进入编辑/打开模板库后才出现共享 EditorPanels 文件。数据源与全局设置面板亦可按需打开。
- 地图拖动中，框从 `20,0,80,74` 变为 `13,5,80,74`，北京标签同时从 `1192,459` 移至 `1096,492`；松手前没有产生历史。按 Esc 后框与标签恢复原位置，撤销仍禁用。地图缩放至 `72×65` 后可一步撤销。
- 修复边界方向键不断写入相同配置的问题。回归覆盖地图/普通组件各 80 次无变化操作：不误报未保存、不挤掉真正修改；真实浏览器中左边界继续向左时撤销保持禁用。
- 1120×718、1920×1080、3840×2160 均无页面水平溢出。4K 稳定后的场景 buffer 为 `2566×1436`，保留基线像素预算、材质和几何质量。
- 390×844 修复 CN ATLAS 与区域标题重叠；标识右边界 76px、区域文字左边界 86px。页面宽度为 390px，地图保持 440px 高。
- 手机编辑器页面与工具栏宽 390px，620px 画布在内部横向滚动，滚动偏移可达 246px；组件库/属性页签可切换。展示页面可滚至运行事件，表格内滚动后最后一行完整出现在视口内。
- 全国 → 湖北 → 十堰 → 丹江口实际切换成功，省级设备指标更新为 232；区县无下级时显示空态。丹江口道路标签含 G70/G59/G209/G241/G316/S77/S57，返回全国可用。
- 全国常规帧为 347 次绘制、575,817 个提交三角面；13:13:28 帧计数为 2，放大/恢复视角后为 4，13:13:51 仍为 4。时钟更新不持续触发地图绘制；不据此推导现场 FPS。
- 测试模板已删除，保存的画布恢复运行总览，临时视口尺寸恢复。最终加载无应用 JavaScript 错误；依赖的 Three.Clock/Three CJS 弃用提示保留。

## 截图证据

| 场景 | 截图 |
|---|---|
| 默认展示 | [1920](evidence/editor-display-1920.jpg)、[1120](evidence/editor-display-1120.jpg)、[4K](evidence/editor-display-4k.jpg) |
| 自由画布与组件 | [编辑器](evidence/editor-canvas-1920.jpg)、[十二组件](evidence/editor-widget-gallery.jpg)、[数据分析模板](evidence/editor-analysis-template.jpg) |
| 手机适配 | [展示](evidence/editor-display-mobile.jpg)、[内部画布](evidence/editor-canvas-mobile.jpg)、[底部事件](evidence/editor-mobile-bottom.jpg) |
| 区县导航 | [丹江口](evidence/editor-danjiangkou.jpg) |

原始参考与基线对照图为 [reference-vs-final.jpg](evidence/reference-vs-final.jpg)，用于核对材质、灯光与整体风格。前一轮结果保留在 [精细化验收](refinement-qa.md)。

## 交付边界

现场设备长期运行、真实业务接口和商业部署仍未验收。窄屏内的密集表格允许手动滚动，未提供自动轮播或承诺所有行始终同屏。

数据接入与模板格式见 [编辑与对接指南](editor-guide.md)。当前模板存储在浏览器，示例接口是固定快照；不包含服务端配置中心、权限、租户隔离、审批发布或多人协作。
